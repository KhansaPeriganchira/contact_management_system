//class create chayuunnu, class create time first letter must uppercase

//which data present in MyContact
// we export this class because  further to  use another place

export class MyContact{

      id ?:string;//id nagathu data vannal string aayirikkum
      name ?:string;
      email ?:string;
      phone ?:string;
      photo ?:string;
      company ?:string;
      title ?:string;
      groupId?:string;
        
                                    
           
}